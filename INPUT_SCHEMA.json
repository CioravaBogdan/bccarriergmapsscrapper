{
  "title": "Google Maps Advanced Scraper Input",
  "description": "Input schema for the advanced Google Maps scraper actor.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Start URLs",
      "type": "array",
      "description": "List of Google Maps URLs to scrape. Leave empty to use search parameters.",
      "editor": "requestListSources",
      "default": []
    },
    "search": {
      "title": "Search Term",
      "type": "string",
      "description": "What to search for on Google Maps (e.g., 'restaurants', 'hotels', 'cafes').",
      "default": "",
      "editor": "textfield"
    },
    "searchLocation": {
      "title": "Search Location",
      "type": "string",
      "description": "The location to search in (e.g., 'New York', 'Paris', 'Tokyo').",
      "default": "",
      "editor": "textfield"
    },
    "latitude": {
      "title": "Latitude",
      "type": "string",
      "description": "Latitude coordinate for area search (e.g., 46.5654079)",
      "editor": "textfield"
    },
    "longitude": {
      "title": "Longitude",
      "type": "string",
      "description": "Longitude coordinate for area search (e.g., 26.9169387)",
      "editor": "textfield"
    },
    "searchRadius": {
      "title": "Search Radius (km)",
      "type": "integer",
      "description": "Radius around coordinates to search in (only applies when coordinates are specified)",
      "default": 5,
      "editor": "number"
    },
    "maxItems": {
      "title": "Maximum Items",
      "type": "integer",
      "description": "Maximum number of results to extract. 0 = unlimited",
      "default": 0,
      "editor": "number"
    },
    "language": {
      "title": "Language",
      "type": "string",
      "description": "Language to use for extraction (e.g., 'en', 'ro')",
      "default": "en",
      "editor": "select",
      "enum": ["en", "ro", "fr", "de", "es", "it"]
    },
    "includeReviews": {
      "title": "Include Reviews",
      "type": "boolean",
      "description": "Whether to extract business reviews",
      "default": false,
      "editor": "checkbox"
    },
    "maxReviews": {
      "title": "Max Reviews per Business",
      "type": "integer",
      "description": "Maximum number of reviews to extract per business. 0 = all available",
      "default": 0,
      "editor": "number"
    },
    "includeReviewerInfo": {
      "title": "Include Reviewer Information",
      "type": "boolean",
      "description": "Whether to extract information about review authors",
      "default": false,
      "editor": "checkbox"
    },
    "maxCostPerRun": {
      "title": "Maximum Cost per Run ($)",
      "type": "integer",
      "description": "Estimated maximum cost. 0 = unlimited",
      "default": 0,
      "editor": "number"
    }
  },
  "required": []
}